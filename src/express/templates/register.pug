extends layout.pug

block content
  main
    h1.visually-hidden Регистрация пользователя
    section
      .popup.popup--registration.popup--anti
        button(class='popup__button button button--popup-close' aria-label='Закрыть окно' onclick='history.back();')
          | Закрыть
          | окно
        h2(class='popup__title') Регистрация
        div(class='popup__form popup__form--active form form--register')
          form(action='/register' method='POST' enctype="multipart/form-data")
            input(type="hidden" name="_csrf" value=csrf)
            div(class='form__field')
              label
                input(type='email' name='email' placeholder='Электронная почта' required='')
            div(class='form__field')
              label
                input(type='text' name='firstName' placeholder='Имя' required='')
            div(class='form__field')
              label
                input(type='text' name='lastName' placeholder='Фамилия')
            div(class='form__field')
              label
                input(type='password' name='password' placeholder='Пароль' required='')
            div(class='form__field')
              label
                input(type='password' name='repeatedPassword' placeholder='Повтор пароля' required='')
            +errorMessages(validationMessages)
            div(class='form__image-loader')
              a(class='form__avatar avatar')
                img(src='/img/icons/smile.svg' alt='аватар пользователя')
              label
                input.visually-hidden(type='file' name='upload')
                | Загрузить фото профиля
            button(class='form__submit-btn form__submit-btn--register button button--colored' type='submit') Зарегистрироваться
