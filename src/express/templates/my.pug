extends layout.pug

block content
  main.main-page.main-page--padding
    section(class='main-page__notes notes')
      h1(class='notes__title') Мои записи
      ul(class='notes__list')
        each article of myArticles
          li(class='notes__list-item')
            time(class='notes__item-time' datetime=`${article.createdAt}`) #{formatDate(article.createdAt)}
            a(class='notes__item-text' href='#') #{article.title}
            a(class='notes__button button button--close-item' href=`/articles/delete/${article.id}`)
              span.visually-hidden Закрыть строку списка
