extends layout.pug

block content
  main
    section.search-page
      div(class=`search-page__wrapper${found.length ? `` : ` search-page__wrapper--empty`}`)
        h1 Поиск
        div(class='search search-page__form')
          form(action='/search' method='get')
            label
              input(type='text' name='query' placeholder='Что ищем?' value=`${query ? query : ``}`)
            button(class='search__button button button--transparent' type='submit') Найти
          each article of found
            div(class='search__result')
              time(class='search__date' datetime='2019-03-21T20:33') #{formatDate(article.createdAt)}
              a(class='search__link') #{article.title}
        if !found.length
          p.search-page__empty Ничего не нашлось

