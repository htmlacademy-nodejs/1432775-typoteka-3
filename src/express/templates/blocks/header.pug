html
  body
    header.header
      a(class='header__logo logo' href='/')
        img(class='logo__image' src='/img/icons/logo-image-only.svg' alt='логотип Тайпотеки')
        span(class='logo__name') Типотека
      if user

        if user.roles.some((role) => role.name === `admin`)
          a(class="button button--colored header__button-new" href="/articles/add") Новая запись
          .header__dropdown
            a.button.button--burger.header__burger(href='#') Открыть меню
            ul.navigation.header__navigation
              li.navigation__item
                a(href='/categories') Категории
              li.navigation__item
                a(href='/my') Публикации
              li.navigation__item
                a(href='/my/comments') Комментарии

        else
          nav.header__nav
            ul.header__nav-list
              li.header__list-item
                p #{`${user.firstName} ${user.lastName}`}
              li.header__list-item
                a.header__exit(href='/logout') Выйти
          a.header__avatar.avatar
            if user.avatar
              img(src=`/img/${user.avatar}` class='header__user-avatar' alt='аватар пользователя')

      else
        nav(class='header__nav')
          ul(class='header__nav-list')
            li(class='header__list-item')
              a(class='header__registration' href='/register') Регистрация
            li(class='header__list-item')
              a(class='header__enter' href='/login') Вход с паролем
      a(class='header__search button button--search' href='/search' aria-label='поиск по сайту')
